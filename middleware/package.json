{
  "name": "middleware",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run --env"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0"
  },
  "engines": {
    "node": "8.10.0"
  },
  "dependencies": {
    "fs": "0.0.1-security",
    "jsforce": "^1.9.2",
    "lodash": "^4.17.15",
    "util": "^0.12.1",
    "xmldom": "^0.6.0"
  }
}
