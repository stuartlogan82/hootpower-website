{
  "name": "middleware-post",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run --env"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0",
    "jsforce": "^1.9.2"
  },
  "engines": {
    "node": "8.10.0"
  },
  "dependencies": {
    "jsforce": "^1.9.2"
  }
}
